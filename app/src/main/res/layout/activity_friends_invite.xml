<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.friendsinvite.FriendsInviteActivity">

    <ImageButton
        android:id="@+id/friendsInvite_home_button"
        android:layout_width="67dp"
        android:layout_height="64dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="172dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_iconunieduk_foreground" />

    <ImageButton
        android:id="@+id/friendsInvite_back_button"
        android:layout_width="62dp"
        android:layout_height="50dp"
        android:layout_marginStart="4dp"
        android:layout_marginTop="48dp"
        android:tint="@color/white"
        app:layout_constraintEnd_toStartOf="@+id/searchFriends_home_button"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_arrowback" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/friendsInvite_list"
        android:layout_width="403dp"
        android:layout_height="622dp"
        android:layout_marginBottom="4dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>